{
  "buildCommand": "mv api myapi && mv vercel.js api/index.js",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ],
  "functions": {
      "maxDuration": 90
  }
}
